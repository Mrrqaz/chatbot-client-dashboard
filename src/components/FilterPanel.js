import React from 'react';
/**
 * Provides controls for filtering the conversation list.
 */
const FilterPanel = () => {
  return (
    <div className="p-6">
      <h2 className="text-lg font-semibold mb-4">Filters</h2>
      {/* Add filter controls here (date, tags, etc.) */}
      <div className="text-gray-500">Filter controls coming soon.</div>
    </div>
  );
};

export default FilterPanel;
